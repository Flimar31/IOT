# Mosquitto minimal config to allow remote connections
# Listener for plain MQTT on all interfaces
listener 1883 0.0.0.0
allow_anonymous true

# Websocket listener (optional)
listener 9001
protocol websockets

# Keep log output to stdout
log_dest stdout
